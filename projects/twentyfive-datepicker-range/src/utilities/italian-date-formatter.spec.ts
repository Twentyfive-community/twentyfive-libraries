import { ItalianDateFormatter } from './italian-date-formatter';

describe('ItalianDateFormatter', () => {
  it('should create an instance', () => {
    expect(new ItalianDateFormatter()).toBeTruthy();
  });
});
