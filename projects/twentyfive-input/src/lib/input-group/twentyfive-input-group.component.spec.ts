import { TwentyfiveInputGroupComponent } from './twentyfive-input-group.component';

describe('TwentyfiveInputGroupComponent', () => {
  it('should create an instance', () => {
    expect(new TwentyfiveInputGroupComponent()).toBeTruthy();
  });
});
