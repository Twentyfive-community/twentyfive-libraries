import { TwentyfiveInputWithIconComponent } from './twentyfive-input-with-icon.component';

describe('TwentyfiveInputWithIconComponent', () => {
  it('should create an instance', () => {
    expect(new TwentyfiveInputWithIconComponent()).toBeTruthy();
  });
});
